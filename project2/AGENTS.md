# Agents

This project uses the **Symphony Protocol**, content lifecycle. Any agent, any vendor:

```
init project2 <role>   # composer | critic | designer | tester | implementer | srtl | orchestrator
```

Load order: `<SYMPHONY_ROOT>/Agent role.md` -> `.agent_profiles/<role>_profile.md` -> `<SYMPHONY_ROOT>/skills/` -> `MEMORY.md` -> `SKILL.md`.